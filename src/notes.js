const notes = [
    {
        id: string,
        title: string,
        createdAt: string,
        updatedAt: string,
        tags: array,
        body: string,
       }
];

 
module.exports = notes;